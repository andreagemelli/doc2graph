from setuptools import find_packages, setup
import os

setup(
    name='doc2graphs',
    packages=find_packages(),
    #install_requires=install_requires,
    version='0.1.0',
    description='Repo to transform Docs to Graphs and perform several tasks on them',
    author='Andrea Gemelli',
    license='MIT',
)
